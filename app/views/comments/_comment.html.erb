<% @article.comments.each do |comment|%>
  <%= comment.commenter %><br>
  <%= comment.body %><br>
  <%= link_to "Destroy", [comment.article, comment], method: :delete, data: {confirm: "Are you sure?"} %><br>
<% end %>
